{
  "name": "your-project-name",
  "version": "1.0.0",
  "scripts": {
    "start": "node server.js",
    "build": "cd client && npm install && npm run build"
  },
  "dependencies": {
    "express": "^4.17.1",
    "path": "^0.12.7"
  }
}